function getSmeltingRecipe(s1, s2, printConsole) {
    var result;
    if (printConsole) {
        console.log("Returning recipe for: " + s1 + " " + s2);
    }
    // code for returning recipe
    return(result);
}